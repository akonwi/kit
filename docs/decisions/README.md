# Focused Decisions

Use this directory for narrower design decisions that do not need a full architecture doc.

Examples:

- cwd behavior within a session
- transcript rendering rules
- footer/status information policy
- slash command parsing behavior
- session picker interaction model
